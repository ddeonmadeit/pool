# Regenerable intermediates (full pool geometry; rebuilt from data/cache/)
data/pools_raw.json
data/pools_addressed.json
data/cache/
data/*.log


__pycache__/
*.pyc

# Appended to continuously while the renovation pass is in flight;
# re-tracked and committed once it completes.
data/pool_reno.jsonl
