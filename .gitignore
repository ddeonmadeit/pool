# Regenerable intermediates (full pool geometry; rebuilt from data/cache/)
data/pools_raw.json
data/pools_addressed.json
data/cache/
data/*.log

# Appended to continuously while the dating run is in flight; re-tracked and
# committed once the pipeline completes.
data/pool_ages.jsonl

__pycache__/
*.pyc
